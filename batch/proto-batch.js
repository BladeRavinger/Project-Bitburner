/** @param {NS} ns */
export async function main(ns) {
 //Goal, repeatedly Grow/weaken/hack a server at max capacity

 // infinate loop

  // Get time required to complete tasks
  // Get threads required for max effect
  
  // Order Actions by Longest to shortest to complete.
  // Calculate delay between Actions for them all to land within 5ms
  
  // Launch each commnad waiting the required delay before launching the next
  // Profit
}